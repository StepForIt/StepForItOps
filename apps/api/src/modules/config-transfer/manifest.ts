import { ModuleManifest } from '@nwm/core';

export const CONFIG_TRANSFER_MANIFEST: ModuleManifest = {
  id: 'config-transfer',
  name: 'Export / Import de configuration',
  description: 'Export et import JSON de toute la configuration',
};
